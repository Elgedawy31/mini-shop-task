# Backend (used by `bun dev` / `bun dev:backend`)
PORT=5001
CORS_ORIGIN=http://localhost:5000

# Dashboard API URL (used by `bun dev` / `bun dev:dashboard`)
VITE_API_BASE_URL=http://localhost:5001

# Docker Compose overrides (optional)
# DASHBOARD_PORT=5000
# BACKEND_PORT=5001
