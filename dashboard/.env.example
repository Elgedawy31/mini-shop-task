# =============================================================================
# Mini Shop — Dashboard environment template
# =============================================================================
#
# SETUP
#   1. cp .env.example .env.development
#   2. Ensure backend is running (default API: http://localhost:5001)
#   3. Optional: copy SUPABASE_URL + SUPABASE_ANON_KEY from backend/.env for Realtime
#
# TEST ADMIN (after backend seed — see root README)
#   admin@minishop.test / Admin12345!
# =============================================================================

VITE_API_BASE_URL=http://localhost:5001

# Optional — live order queue updates (same anon key as backend; safe in browser)
# VITE_SUPABASE_URL=https://your-project-ref.supabase.co
# VITE_SUPABASE_ANON_KEY=your-anon-key
